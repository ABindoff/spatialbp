% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/easy_wrappers.R
\name{fit_spatial_boundary}
\alias{fit_spatial_boundary}
\title{High-Level Spatial Boundary Fitting}
\usage{
fit_spatial_boundary(
  df,
  y1,
  y2 = NULL,
  latent_type = "radial",
  center = NULL,
  iterations = 3000,
  latent_col = NULL,
  lambda = 0
)
}
\arguments{
\item{df}{A data.frame containing spatial coordinate columns (x, y) and protein intensities.}

\item{y1}{Character, column name of the first protein channel.}

\item{y2}{Character (optional), column name of the second protein channel for joint modeling.}

\item{latent_type}{Character, type of boundary geometry to construct: `"radial"` (circular boundary around center) or `"linear"` (linear boundary along X-axis). Default is `"radial"`.}

\item{center}{Numeric vector of length 2, the center coordinates for the radial boundary (optional, defaults to geometric center of the coordinates).}

\item{iterations}{Integer, number of MCMC iterations. Default is `3000`.}

\item{latent_col}{Character (optional), column name of a pre-calculated latent environmental gradient (e.g. `"dist"`, `"elev"`, or `"depth"`). If provided, `latent_type` is ignored.}

\item{lambda}{Numeric or numeric vector of length 2 (optional), Laplace regularization penalty parameter to shrink noise channels to 0. Default is `0.0`.}
}
\value{
A `spatialbp_fit` object containing estimated boundaries and MCMC traces.
}
\description{
A biologist-friendly wrapper that automatically calibrates, runs, and fits
spatial boundary models without requiring manual tuning of MCMC proposal settings.
Exposes L1 regularization (Laplace priors) to filter out noise in multi-boundary datasets.
}
